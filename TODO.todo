
* Apply all fixes form Security Audit 2
* Change documentation so that it adds stores to Express in QuickStart, GUIDE.md explains the "longer" version

JSON:API
* Make pagination use the proper JSON:API format
* Defice if we want to just embrace the JSON:API format, and then provide a layer to convert it to a more traditional one
